name: my workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Perform action on HTML file
        run: |
          # Add your commands here
              Process the cryptoanalysis.html file
      
  
